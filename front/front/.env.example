VITE_API_URL=http://localhost:8000/api

# Notes:
# - Vite uses `VITE_` prefix for env vars. Rename to `.env` or `.env.local` and restart dev server.
# - If your Laravel server runs on a different port, update the URL accordingly.
